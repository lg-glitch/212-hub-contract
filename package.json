{
  "name": "@212/hub-contract",
  "version": "1.0.0",
  "description": "The wire contract between 212 Residential Services and the 212 Hub. Types only — no runtime dependencies.",
  "license": "UNLICENSED",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
